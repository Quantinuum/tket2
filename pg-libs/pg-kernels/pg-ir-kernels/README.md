# pg-ir-kernels

This crate provides basic rewrite operations for `PauliGraph`. It also defines the `PGTableau` trait, which allows rewrites that require a tableau backend, such as conjugating Pauli rotations by Clifford gates.
